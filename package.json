{
  "name": "in-good-hands",
  "version": "2.0.0",
  "private": true,
  "workspaces": [
    "shared",
    "client",
    "server"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace=client",
    "dev:server": "npm run dev --workspace=server",
    "dev:server:infisical": "infisical run --env=dev -- npm run dev --workspace=server",
    "dev:mobile": "npm --prefix mobile start"
  }
}
